//! Cursor-format output (.cursorrules).

use contextbridge_core::{AppError, ContextFormatter, ProjectContext};
use std::path::PathBuf;

/// Formatter that produces a `.cursorrules` file for Cursor IDE.
pub struct CursorFormatter;

impl ContextFormatter for CursorFormatter {
    /// Render the project context as a .cursorrules document.
    fn format(&self, _ctx: &ProjectContext) -> Result<String, AppError> {
        // TODO: implement Cursor-specific format
        Ok(String::from("# Cursor rules — auto-generated by ContextBridge\n"))
    }

    /// Output file name.
    fn output_filename(&self) -> &str {
        ".cursorrules"
    }

    /// Output directory (project root).
    fn output_directory(&self) -> PathBuf {
        PathBuf::from(".")
    }
}
